NEXT_PUBLIC_USER_POOL_ID=
NEXT_PUBLIC_USER_POOL_CLIENT_ID=
NEXT_PUBLIC_AWS_PROJECT_REGION=
NEXT_PUBLIC_AWS_CONGNITO_REGION=

# Public because the profile-photo upload flow calls the API directly from
# the browser; the API itself is protected by the Cognito access token on
# every request, not by keeping this URL secret.
NEXT_PUBLIC_API_URL=http://localhost:4000
